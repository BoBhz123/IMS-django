# Used only for the production build served directly by the Django/Heroku app at the
# same origin (see ims/urls.py's SPA catch-all + WHITENOISE_ROOT). This is the "public"
# tenant view — tenant-specific subdomains aren't configured as Heroku custom domains,
# so this build only serves the public (empty, by design) schema at the bare hostname.
VITE_API_BASE_URL=https://ims-tenant-app-54080dc67033.herokuapp.com
