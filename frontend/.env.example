# Leave unset to auto-derive from the page's own host (works for both localhost and LAN access,
# e.g. opening the Vite dev server's network URL from a phone). Set explicitly only to override.
# VITE_API_BASE_URL=http://localhost:8000
